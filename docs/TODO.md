# 待办任务

## 功德气泡锚点:时间文字顶部

- 状态:待办(2026-08-14 确认)
- 需求:功德彩蛋气泡的锚点应为**时间文字(HH:mm 那一行)的顶部**,从时间文字顶部冒泡并上升。
- 现状:`v1.1.1`(commit c86b0fc)当前锚在**日期文字顶部**(`ClockWidget.kt` 中 `dateAnchorX/Y` 记录的是日期行)。用户澄清该方向不对,要求改回时间文字顶部。
- 注意:横屏下时间文字是 Start 对齐(靠左),不能用时钟组件 Box 的 `TopCenter`(旧实现即此,横屏锚点偏右导致"位置不对");需用 `onGloballyPositioned` 记录时间 BasicText 自身的顶部中心。
